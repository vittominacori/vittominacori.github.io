{
  "name": "vittominacori.github.io",
  "version": "1.0.0",
  "private": true,
  "description": "Vittorio Minacori github page",
  "scripts": {
    "lint": "prettier --check .",
    "lint:fix": "prettier --write --list-different .",
    "dev": "export NODE_OPTIONS=--openssl-legacy-provider && vuepress dev",
    "build": "export NODE_OPTIONS=--openssl-legacy-provider && vuepress build",
    "deploy": "export NODE_OPTIONS=--openssl-legacy-provider && vuepress build && sh scripts/deploy.sh"
  },
  "author": "Vittorio Minacori (@vittominacori)",
  "contributors": [
    {
      "name": "Vittorio Minacori",
      "url": "https://github.com/vittominacori"
    }
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/vittominacori/vittominacori.github.io.git"
  },
  "homepage": "https://vittominacori.github.io",
  "bugs": {
    "url": "https://github.com/vittominacori/vittominacori.github.io/issues"
  },
  "license": "MIT",
  "devDependencies": {
    "prettier": "^3.3.2",
    "vue-cookie-law": "^1.13.3",
    "vuepress": "^1.9.10",
    "vuepress-plugin-gtag": "vittominacori/vuepress-plugin-gtag"
  }
}
